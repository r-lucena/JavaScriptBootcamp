// Imprime en la consola los números del 0 al -10.
/**
 * Creamos una variable numérica de valor 0
 * El loop continuará mientras la variable sea mayor o igual que -11
 * El loop funcionará solo cuando la condición 'number >= -11' sea cierta
 */
for(let number = 0; number >= -10; number = number - 1){
    console.log(number);
}