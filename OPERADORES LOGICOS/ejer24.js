// Reescribe el ejercicio 24 utilizando el bucle while.

// Creamos una variable numérica de valor 0.
let number = 0;
// Creamos el loop while con la condición de que la variable "number" sea menor o igual a "10".
while (number <= 10) {
    // Imprimimos en la consola el mensaje "The number is " y el valor actual de la variable "number".
  console.log("The number is " + number);
  // Aumentamos el valor de la variable "number" en 1 por cada iteración.
  number++;
}
