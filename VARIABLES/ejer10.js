const firstYearCompleted = true;
const yearsCompleted = 1;

yearsCompleted == firstYearCompleted
yearsCompleted === firstYearCompleted

/**
 * La diferencia entre == y === la encontramos en que: 
 * El operador == (Operador de Igualdad) verifica que las dos variables tengan el mismo valor y que por tanto que sean comparables
 * En cambio === (Operador de Igualdad Estricta) verifica que el valor y el tipo de las variables sean iguales
 */